# Deploy no Render: build na raiz, app roda em app/
services:
  - type: web
    name: reservation-audit-engine
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: cd app && gunicorn --bind 0.0.0.0:$PORT app:app
